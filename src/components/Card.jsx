import { useContext } from "react";

import GameContext from "../context/GameContext";

const Card = ({ card }) => {
    const {} = useContext(GameContext);

    return <div></div>;
};

export default Card;
